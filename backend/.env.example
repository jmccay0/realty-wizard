# Server Configuration
PORT=8080

# Authentication
AUTH_ENABLED=true
JWT_SECRET=your-secret-key-change-in-production-minimum-32-characters

# Database (defaults to ./data/realty-wizard.db if not specified)
# DB_PATH=./data/realty-wizard.db
